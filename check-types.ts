/// <reference path="kanjidate.d.ts" />

import * as kanjidate from "kanjidate"

let s = kanjidate.format()
let ss = kanjidate.format(new Date())
let sss = kanjidate.format("2017-03-23")
let t:string = kanjidate.f1
